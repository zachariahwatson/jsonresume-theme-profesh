{{#if resume.certificates.length}}
	<section id="certifications">
		<h2>Certifications</h2>
        {{#each resume.certificates}}
            {{#> item title=name subtitle=issuer endDate=date url=url mainUrl='y'}}
            {{/item}}
        {{/each}}
    </section>
{{/if}}