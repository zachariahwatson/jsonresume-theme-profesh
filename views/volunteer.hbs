{{#if resume.volunteer.length}}
    <section id="volunteer">
        <h2>Volunteering</h2>
        {{#each resume.volunteer}}
            {{#> item title=organization subtitle=position startDate=startDate endDate=endDate}}
                {{#if highlights.length}}
                    <ul class="highlights">
                        {{#each highlights}}
                            <li>{{.}}</li>
                        {{/each}}
                    </ul>
                {{/if}}
            {{/item}}
        {{/each}}
    </section>
{{/if}}
